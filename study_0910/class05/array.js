import React, {useState} from 'react';

export function Array() {
    const array = [{id:1, text:'Apple'}, {id:2, text:'Banana'}, {id:3, text:'Kiwi'}]

    const list = nuber.map((id) =>
        <li key={key.toString()}>{text}</li>
    );

    return (
        <div>
            <h1>[ARRAY]</h1>
            
        </div>

    );
}